[project]
name = "orca"
version = "0.1.0"
description = "orca serving engine"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.2",
    "safetensors>=0.7.0",
    "tokenizers>=0.22.2",
    "torch>=2.10.0",
]
